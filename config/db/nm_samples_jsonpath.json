{
    "jsonpaths": [
      "$['id']",
      "$['event_type']",
      "$['timestamp']",
      "$['user']",
      "$['client']",
      "$['sample_name']",
      "$['sample_value']",
      "$['sample_string']",
      "$['sample_float']"
    ]
}
